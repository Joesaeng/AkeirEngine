# build outputs
/build/
/out/
# 의존성 소스 캐시 (scripts/fetch-deps.ps1 로 재생성 가능)
/.cpm-cache/
# 파생 캐시 (설계 문서 §5: 삭제해도 재생성 가능해야 한다)
Cache/
Game/Cache/
# 테스트 결과 (§24: Tests/.results/<run>/results.json + artifacts)
.results/
# 릴리즈 zip 스테이징 (scripts/package.py)
/dist/
# IDE
/.vs/
/.vscode/
/.idea/
*.user
# OS
Thumbs.db
.DS_Store

# 리서치 작업 자료는 공개 저장소에 넣지 않는다 (C:\Project\Project_ME_research 에 별도 보관)
/research/

# MCP client config is machine-specific: copy .mcp.json.example or run `akeir mcp --print-config > .mcp.json`
.mcp.json

# 엔진 구현 규약·판단 기준 (로컬 전용, 사용자 소유 — 수정은 사용자와 먼저 합의한 뒤에만)
/Docs/PRINCIPLES.md
